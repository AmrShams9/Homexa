<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تسجيل الدخول - Homeexa</title>
    
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Helvetica+Neue:wght@400;700&display=swap');

        body {
            font-family: 'Helvetica Neue', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #7ebee1;
            color: #34507c;
            text-align: center;
        }

        .container {
            width: 100%;
            max-width: 400px;
            margin: 100px auto;
            padding: 20px;
            background: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        h2 {
            color: #f15a29;
        }

        form {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        input {
            padding: 10px;
            font-size: 1em;
            border: 1px solid #34507c;
            border-radius: 5px;
            outline: none;
            text-align: right;
        }

        button {
            padding: 10px;
            background-color: #f15a29;
            color: white;
            border: none;
            font-size: 1em;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #d64921;
        }

        .back-link {
            margin-top: 15px;
        }

        .back-link a {
            text-decoration: none;
            color: #34507c;
            font-weight: bold;
        }
        .logo {
        width: 80px; /* Adjust size as needed */
        height: auto; /* Maintain aspect ratio */
        display: block;
        margin: 10px auto; /* Add spacing */
    }
    </style>
</head>
<body>

    <div class="container">
        <h2>تسجيل الدخول إلى Homexa</h2>
        <img src="\OrangeX.png" alt="HomeexaLogo" class="logo">
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="أدخل اسم المستخدم" required>
            <input type="password" name="password" placeholder="أدخل كلمة المرور" required>
            <button type="submit">تسجيل الدخول</button>
        </form>
        
    </div>

</body>
</html>
